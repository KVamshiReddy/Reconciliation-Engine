spring:
  application:
    name: Reconciliation-Engine
  datasource:
    url: jdbc:postgresql://localhost:5433/reconciliation
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: reconciliation-group
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      enable-auto-commit: false
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

server:
  port: 8080

reconciliation:
  rounding-threshold: 0.00500000
  critical-break-threshold: 0.10000000
  auto-resolve-enabled: true
  settlement-window-close: "18:00:00"
  thread-pool-size: 0

resilience4j:
  retry:
    instances:
      ledgerService:
        max-attempts: 3
        wait-duration: 1s
        exponential-backoff-multiplier: 2
        retry-exceptions:
          - org.springframework.dao.TransientDataAccessException
          - java.net.SocketTimeoutException
      settlementSimulator:
        max-attempts: 3
        wait-duration: 1s
        exponential-backoff-multiplier: 2

  circuitbreaker:
    instances:
      ledgerService:
        sliding-window-size: 10
        failure-rate-threshold: 50
        wait-duration-in-open-state: 30s
        permitted-number-of-calls-in-half-open-state: 3

management:
  endpoints:
    web:
      exposure:
        include: health, info, prometheus, metrics
      endpoint:
        prometheus:
          enabled: true
      metrics:
        export:
          prometheus:
            enabled: true

alpaca:
  api-key: PKZS3WYRLUMWMKVCIMIPHTM7EK
  api-secret: 5aapZcpX8EPbY69DnuXKahPoru3k9jBRVXzAowiMb5YS
  base-url: https://paper-api.alpaca.markets
  enabled: true